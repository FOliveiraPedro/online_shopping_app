import 'package:flutter/material.dart';

class CategoryFlag extends StatelessWidget {
  const CategoryFlag({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
